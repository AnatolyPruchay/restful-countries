import {Controls} from '../../features';
import {CountryList} from '../../features';

export const HomePage = (): JSX.Element => {
  return (
    <>
      <Controls/>
      <CountryList/>
    </>
  );
};
